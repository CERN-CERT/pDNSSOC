require 'rake'

# Define task to clean installation files
task :clean do
  puts "Cleaning the room."
  # Place the original script content here to clean the installation files
end

# Define task to install system packages and pDNSSOC files
task :install do
  puts "Installing system packages."

  # Place the original script content here to install system packages

  puts "Moving pDNSSOC files around."

  # Place the original script content here to move pDNSSOC files
end

# Define task to start Fluentd
task :start_fluentd do
  puts "Starting Fluentd."

  # Place the original script content here to start Fluentd
end

# Define a default task that depends on the other tasks
task default: [:clean, :install, :start_fluentd]
